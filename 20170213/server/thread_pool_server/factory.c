#include"factory.h"

void  factory_init(pfac p,pthfunc entry)
{


void* threadfunc(void* p)
{
	pfac pf = (pfac)p;
	pque pq = &p->que;
	while(1)
	{
		pthread_mutex_lock(&pq-mutex);

		typedef struct factory{
		}

		void que_init(pque p)
		{
			memset(p,0,sizeof(pque));
		}

		int main(int argc char* argv[])
		{

 			if(argc!=5)
			{

				}



